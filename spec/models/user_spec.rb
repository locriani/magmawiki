require 'spec_helper'

describe User do
  context "preferences" do
    it "should load default preferences for uninitialized keys" do
      pending
    end
    
    it "should silently serialize set preferences to the database" do
      pending
    end
  end
end
